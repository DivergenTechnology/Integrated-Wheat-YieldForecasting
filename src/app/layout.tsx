import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";
import { Toaster } from "@/components/ui/toaster";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "UAV Yield Forecast | Precision Farming Analytics",
  description:
    "Forecast expected yields for specific farming zones using UAV vegetation indices (NDVI, GNDVI, NDRE, LAI, canopy cover) from drone multispectral surveys.",
  keywords: ["UAV", "drone", "NDVI", "yield forecast", "precision agriculture", "farming zones", "remote sensing"],
}

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        {children}
        <Toaster />
      </body>
    </html>
  );
}
